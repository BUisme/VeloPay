# 💰 Loan Payment System

ระบบจัดการเงินกู้ + การผ่อนชำระ พร้อม QR Code, PDF, Customer Portal

---

## 📂 โครงสร้างไฟล์

```
loan-payment-system/
├── index.html              ← แดชบอร์ด (หน้าหลัก)
├── loan-form.html          ← เพิ่มสัญญาใหม่
├── loan-list.html          ← รายการสัญญา (ค้นหา + Filter)
├── loan-detail.html        ← รายละเอียดสัญญา + บันทึกชำระ
├── customer-view.html      ← หน้าลูกค้า (สาธารณะ, ไม่ต้อง login)
├── schema.sql              ← SQL สำหรับสร้าง Database บน Supabase
├── css/
│   └── main.css            ← Stylesheet ทั้งหมด
├── js/
│   ├── config.js           ← Supabase URL + Key (⚠️ แก้ไขก่อน deploy)
│   ├── utils.js            ← Utility (format เงิน, วันที่, toast, etc.)
│   ├── calculator.js       ← คำนวณดอกเบี้ย (Reducing + Flat)
│   ├── database.js         ← CRUD กับ Supabase
│   ├── pdf-generator.js    ← สร้าง PDF (ตารางผ่อน, ใบเสร็จ, รายงาน)
│   └── qrcode-generator.js ← สร้าง QR Code
└── README.md               ← ไฟล์นี้
```

---

## 🚀 วิธีเปิดใช้งาน (ทำ 3 ขั้นตอน)

### ขั้นตอนที่ 1 – สร้าง Database บน Supabase

1. ไปที่ **https://supabase.com** → สมัкรหรือ Log in
2. กด **New Project** → ใส่ชื่อ project → เลือก region → กด Create
3. รอ project พร้อม (ประมาณ 30 วินาที)
4. ไปที่ **SQL Editor** (ด้านซ้าย sidebar)
5. เปิดไฟล์ **schema.sql** แล้ว copy ทั้งหมด → paste ใน SQL Editor → กด **Run**
6. ตรวจสอบใน **Table Editor** ว่ามี table `loans` กับ `payments`

### ขั้นตอนที่ 2 – แก้ไข config.js

1. ใน Supabase ไปที่ **Settings → API**
2. Copy **Project URL** → แล้วใส่ใน `js/config.js` ที่บรรทัด `SUPABASE_URL`
3. Copy **anon** key (ใน Public section) → แล้วใส่ใน `SUPABASE_ANON_KEY`

```js
// js/config.js
const SUPABASE_URL     = 'https://your-project.supabase.co';  // ← ใส่ตรงนี้
const SUPABASE_ANON_KEY = 'eyJhbGciO...';                      // ← ใส่ตรงนี้
```

### ขั้นตอนที่ 3 – Deploy บน GitHub Pages

1. สร้าง **GitHub Repository** ใหม่ (เช่น `loan-payment-system`)
2. Upload ไฟล์ทั้งหมดเข้า repository
3. ไปที่ **Settings → Pages**
   - Source: `Deploy from a branch`
   - Branch: `main` (หรือ `master`)
   - Folder: `/ (root)`
4. กด **Save** → รอ deploy (ประมาณ 1-2 นาที)
5. เปิด URL ที่ GitHub ให้มา เช่น `https://username.github.io/loan-payment-system/`

> ⚠️ **อย่าลืม** แก้ไข `js/utils.js` บรรทัด `getCustomerViewUrl` ใส่ GitHub username ของตัวเอง:
> ```js
> const baseUrl = 'https://YOUR_GITHUB_USERNAME.github.io/loan-payment-system/customer-view.html';
> ```

---

## ✅ ฟีเচาร์ที่มี

| ฟีเชอร์ | คำอธิบาย |
|---|---|
| แบบดอกเบี้ย 2 แบบ | ลดต้นลดดอก (Reducing) + คงที่ (Flat Rate) |
| ความถี่การจ่าย | รายเดือน / รายสัปดาห์ / รายวัน |
| Preview แบบ Real-time | พิมพ์ข้อมูลแล้วเห็นการคำนวณทันที |
| บันทึกชำระ | พร้อม partial payment (จ่ายบางส่วน) |
| QR Code | สร้าง, Download PNG, Copy URL |
| PDF 3 แบบ | ตารางผ่อน, ใบเสร็จ, รายงานสรุป |
| Customer Portal | ลูกค้าดูสถานะเงินกู้เอง (สาธารณะ) |
| Search + Filter | ค้นหาสัญญา + กรองสถานะ/แบบดอกเบี้ย |
| Progress Bar | แสดง % การชำระ |
| Thai UI | วันที่พ.ศ., สกุลเงินบาท, เดือนภาษาไทย |
